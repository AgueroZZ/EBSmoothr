% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_spatial_scores.R
\name{spatial_select}
\alias{spatial_select}
\title{Select spatial versus reference components}
\usage{
spatial_select(
  object,
  locations,
  spatial = list(family = "matern", link = "identity"),
  reference = list(family = "constant"),
  method = c("score", "permutation"),
  tol = 1e-06,
  alpha = 0.05,
  ...
)
}
\arguments{
\item{object}{A numeric vector, matrix/data frame, flash-like list, or
flashier object containing component loadings.}

\item{locations}{Spatial coordinates for Matern fits.}

\item{spatial}{Named list of spatial \code{\link[=eb_smoother]{eb_smoother()}} arguments.}

\item{reference}{Named list of reference \code{\link[=eb_smoother]{eb_smoother()}} arguments. Defaults
to \code{list(family = "constant")}.}

\item{method}{Selection method: \code{"score"} or \code{"permutation"}.}

\item{tol}{Score-difference tolerance for \code{method = "score"}.}

\item{alpha}{One-sided p-value cutoff for \code{method = "permutation"}.}

\item{...}{Additional arguments passed to \code{\link[=spatial_scores]{spatial_scores()}} for
\code{method = "score"} or to \code{\link[=spatial_scores_permutation]{spatial_scores_permutation()}} for
\code{method = "permutation"}; examples include \code{components}, \code{B}, \code{seed},
\code{refit}, and \code{ncores}.}
}
\value{
A list of class \code{"spatial_select"} with a \code{selection_summary} data
frame. Score mode also returns \code{scores} and, when kept, fit lists.
Permutation mode returns the \code{permutation} result from
\code{\link[=spatial_scores_permutation]{spatial_scores_permutation()}}.
}
\description{
Classifies each component as spatial or reference using either a direct score
comparison or a permutation p-value for the spatial score.
}
\details{
With \code{method = "score"}, the function calls \code{\link[=spatial_scores]{spatial_scores()}} and selects
the spatial model when \code{spatial_score - reference_score > tol}. It selects
the reference route when the difference is below \code{-tol}; otherwise the row is
marked as a tie.

With \code{method = "permutation"}, the function calls
\code{\link[=spatial_scores_permutation]{spatial_scores_permutation()}} and selects the spatial model when the
one-sided permutation p-value is at most \code{alpha}. The \code{reference} family is
still recorded as the fallback selected family for components that do not
pass the permutation screen.

The \code{spatial} list may request a Matern log-link fit via
\code{spatial = list(family = "matern", link = "log")}; all other Matern options,
including PC-prior options such as \code{pc.penalty}, are passed through to
\code{\link[=eb_smoother]{eb_smoother()}}.
}
\examples{
loc <- seq(0, 1, length.out = 12)
X <- cbind(sin(2 * pi * loc), rnorm(length(loc)))
selection <- spatial_select(
  X,
  locations = loc,
  spatial = list(family = "matern", link = "identity"),
  reference = list(family = "constant"),
  method = "score"
)
selection$selection_summary[, c("factor_k", "selection", "selected_family")]

}
