% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_spatial_scores.R
\name{spatial_scores}
\alias{spatial_scores}
\title{Score components under spatial and reference smoothers}
\usage{
spatial_scores(
  object,
  locations = NULL,
  components = NULL,
  spatial = list(family = "matern", link = "identity"),
  reference = NULL,
  keep_fits = TRUE,
  ...
)
}
\arguments{
\item{object}{A numeric vector, matrix/data frame, flash-like list, or
flashier object containing component loadings. Matrix columns are treated
as components.}

\item{locations}{Spatial coordinates used for Matern fits unless
\code{spatial$locations} or \code{spatial$setup} is supplied.}

\item{components}{Optional component indices or names to score.}

\item{spatial}{Named list of spatial \code{\link[=eb_smoother]{eb_smoother()}} arguments. Defaults to
\code{list(family = "matern", link = "identity")}.}

\item{reference}{Optional named list of reference \code{\link[=eb_smoother]{eb_smoother()}} arguments.
If supplied without \code{family}, \code{family = "constant"} is used.}

\item{keep_fits}{If \code{TRUE}, return fitted \code{eb_smoother_fit} objects.}

\item{...}{Additional \code{\link[=eb_smoother]{eb_smoother()}} arguments used as defaults for both
\code{spatial} and \code{reference}; model-specific options should usually be placed
directly inside the corresponding list.}
}
\value{
A list of class \code{"spatial_scores"} with fields:
\describe{
\item{scores}{A data frame with one row per component. Important columns
include \code{spatial_score}, \code{reference_score}, \code{delta},
\code{spatial_score_semantics}, \code{reference_score_semantics},
\code{spatial_laplace_curvature}, \code{matern_range}, \code{matern_sigma}, and
point-reference summaries when applicable.}
\item{fits}{When \code{keep_fits = TRUE}, a list with \code{spatial} and
\code{reference} fit lists.}
\item{screening_summary}{Alias of \code{scores} for simulation workflows.}
\item{matern_fits,nonspatial_fits}{Compatibility aliases for spatial and
reference fits when \code{keep_fits = TRUE}.}
}
}
\description{
Fits each selected component under a spatial smoother and, optionally, a
reference smoother, then returns the fitted scores in a component-level table.
}
\details{
\code{spatial_scores()} is a public wrapper around \code{\link[=eb_smoother]{eb_smoother()}} for the common
workflow of scoring factor/loadings columns from a fitted factorization. The
input \code{object} may be a numeric vector, a matrix/data frame whose columns are
components, a flash-like list with \code{L_pm}, or a flashier object whose
posterior mean loadings can be extracted.

The spatial score is \code{as.numeric(fit$log_likelihood)} from the spatial
\code{\link[=eb_smoother]{eb_smoother()}} fit. For Matern fits this is the marginal log likelihood, or
the PC-penalized marginal log likelihood when \code{spatial$pc.penalty} is
supplied. The score semantics for each fit are reported in
\code{spatial_score_semantics} and \code{reference_score_semantics}.

The \code{spatial} list is passed to \code{\link[=eb_smoother]{eb_smoother()}} after adding \code{family = "matern"} by default. Use \code{spatial = list(family = "matern", link = "log")}
for positive smooth signals on the log link; with \code{backend = "auto"} this
uses Fisher Laplace scoring and reports \verb{laplace_fisher_<beta_mode>} score
semantics. The \code{reference} list, when supplied, supports
\code{family = "constant"}, \code{"point_exponential"},
\code{"point_normal"}, and \code{"point_laplace"}. The old name
\code{family = "nonspatial"} is intentionally rejected; use \code{family = "constant"}
for the constant Gaussian reference.
}
\examples{
loc <- seq(0, 1, length.out = 12)
X <- cbind(sin(2 * pi * loc), rnorm(length(loc)))
scores <- spatial_scores(
  X,
  locations = loc,
  spatial = list(family = "matern", link = "identity"),
  reference = list(family = "constant")
)
scores$scores[, c("factor_k", "spatial_score", "reference_score", "delta")]

positive_scores <- spatial_scores(
  exp(X[, 1]),
  locations = loc,
  spatial = list(family = "matern", link = "log"),
  reference = list(family = "point_exponential")
)
positive_scores$scores$spatial_score_semantics

}
