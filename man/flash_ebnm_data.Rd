% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_krige.R
\name{flash_ebnm_data}
\alias{flash_ebnm_data}
\title{Reconstruct the ebnm problem of one flashier factor}
\usage{
flash_ebnm_data(fl, k, n = 1)
}
\arguments{
\item{fl}{A flash object (from \code{flashier}).}

\item{k}{Index of the factor.}

\item{n}{Dimension carrying the smoothing prior: \code{1} for loadings, \code{2} for factors.}
}
\value{
A list with elements \code{x}, \code{s} and \code{g}.
}
\description{
Returns the data \code{x}, standard errors \code{s} and fitted prior \code{g} that flashier's last
update of factor \code{k} passes to the prior of dimension \code{n}, recomputed from the final
state of the fit. Pass the result to \code{\link[=krige_GP]{krige_GP()}} to get posterior standard
deviations and intervals on a new grid.

The reconstruction uses the final state of the fit, so it matches the data of the
factor's last update only once the fit has converged: after \code{flash_backfit()} (or
\code{flash(..., backfit = TRUE)}). After greedy fitting alone, the early factors were fit
before the later ones existed, and their reconstructed data will differ.

Only dense data matrices are supported (constant, row-wise, column-wise or full
residual precisions).
}
