% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_spatial_scores.R
\name{spatial_scores_permutation}
\alias{spatial_scores_permutation}
\title{Compute permutation scores for a spatial smoother}
\usage{
spatial_scores_permutation(
  object,
  locations,
  B = 100,
  spatial = list(family = "matern", link = "identity"),
  components = NULL,
  refit = FALSE,
  ncores = 1,
  seed = NULL,
  keep_fits = FALSE,
  ...
)
}
\arguments{
\item{object}{A numeric vector, matrix/data frame, flash-like list, or
flashier object containing component loadings.}

\item{locations}{Spatial coordinates for Matern fits.}

\item{B}{Number of random permutations.}

\item{spatial}{Named list of spatial \code{\link[=eb_smoother]{eb_smoother()}} arguments. Defaults to
\code{list(family = "matern", link = "identity")}.}

\item{components}{Optional component indices or names to score.}

\item{refit}{If \code{FALSE}, reuse the observed fit's Matern parameters with
\code{fix_g = TRUE}. If \code{TRUE}, refit Matern parameters for every permuted data
set.}

\item{ncores}{Number of cores for permutation fits. On Unix-like platforms,
values greater than one use \code{parallel::mclapply}; otherwise serial
evaluation is used with a warning.}

\item{seed}{Optional random seed used to generate the permutations.}

\item{keep_fits}{If \code{TRUE}, return observed and permutation fit objects.}

\item{...}{Additional \code{\link[=eb_smoother]{eb_smoother()}} arguments used as defaults unless
overridden by \code{spatial}.}
}
\value{
A list of class \code{"spatial_scores_permutation"} with fields:
\describe{
\item{summary}{One row per component with observed score, p-value, number
of valid permutations, fitted Matern parameters, and score semantics.}
\item{permutation_scores}{One row per component and permutation.}
\item{observed_scores}{Observed-score table before p-value calculation.}
\item{matern_fits}{Observed spatial fits, useful for downstream smoothing
of selected components.}
\item{fits}{When \code{keep_fits = TRUE}, observed and permutation fit lists.}
}
}
\description{
Computes observed spatial scores and permutation-null spatial scores by
randomly breaking the association between component values and locations.
}
\details{
\code{spatial_scores_permutation()} first fits each selected component under the
requested spatial model and records the observed spatial score. It then
permutes the component vector \code{B} times relative to the fixed locations and
re-scores with fixed Matern parameters by default. Set \code{refit = TRUE} to
refit the spatial parameters for every permuted data set.

The current spatial model is Matern. Use \code{spatial = list(family = "matern", link = "log")} for positive smooth signals. With the default
\code{refit = FALSE}, the
observed fit's Matern range and marginal standard deviation are supplied as
\code{g_init} with \code{fix_g = TRUE}; if the user also supplies \code{spatial$fix_params},
those values are merged with the permutation fixed-parameter behavior. The
observation noise and intercept are still fitted according to the selected
\code{\link[=eb_smoother]{eb_smoother()}} semantics.

The p-value is the one-sided upper-tail permutation p-value
\verb{number of permutation scores >= observed score / number of valid permutation scores}. The observed score is not added to the permutation-null
scores. Larger scores indicate better support for the spatial model.
}
\examples{
loc <- seq(0, 1, length.out = 10)
x <- sin(2 * pi * loc) + rnorm(length(loc), sd = 0.1)
perm <- spatial_scores_permutation(
  x,
  locations = loc,
  B = 3,
  seed = 1,
  spatial = list(family = "matern", link = "identity")
)
perm$summary[, c("factor_k", "observed_score", "p_value")]

}
