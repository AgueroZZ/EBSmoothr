% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_krige.R
\name{krige_flash}
\alias{krige_flash}
\title{Posterior means of flashier loadings (or factors) on a new grid}
\usage{
krige_flash(
  fl,
  setup,
  new_locations,
  n = 1,
  method = c("analytical", "fem"),
  normalize = c("none", "ldf"),
  kset = NULL,
  boundary = NULL
)
}
\arguments{
\item{fl}{A flash object (from \code{flashier}).}

\item{setup}{The \code{\link[=LGP_setup]{LGP_setup()}} or \code{\link[=Matern_setup]{Matern_setup()}} object used to build the prior of
dimension \code{n}; or a list of such setups, one per factor.}

\item{new_locations}{Numeric vector (1-d) or matrix of new locations.}

\item{n}{Which dimension carries the smoothing prior: \code{1} for loadings (rows of the
data matrix, \code{fl$L_pm}), \code{2} for factors (\code{fl$F_pm}).}

\item{method}{\code{"analytical"} (default) or \code{"fem"}.}

\item{normalize}{\code{"none"} (default) returns the curves on flashier's scale. \code{"ldf"}
divides each curve by the Euclidean norm of the factor's posterior means at the
observed locations, so that it matches \code{flashier::ldf(fl, type = "2")} there.}

\item{kset}{Indices of the factors to evaluate (default: all).}

\item{boundary}{Matern only: boundary treatment for \code{method = "analytical"}, as in
\code{\link[=krige_GP]{krige_GP()}} (default \code{"neumann"} for 1-d priors).}
}
\value{
A matrix with one row per new location and one column per factor in \code{kset}.
Factors that are identically zero give a column of zeros.
}
\description{
A lightweight companion to \code{\link[=krige_GP]{krige_GP()}} for flashier fits whose loadings (or factors)
have an EBSmoothr L-GP or Matern prior. It needs only the flash object, the setup and
the new locations, and returns the posterior mean of every factor there. It works
from the posterior means and fitted priors that flashier stores, so no data are
reconstructed; for standard deviations and intervals use \code{\link[=flash_ebnm_data]{flash_ebnm_data()}} with
\code{\link[=krige_GP]{krige_GP()}}.

For \code{method = "analytical"} the mean is
\eqn{X(t^*)\beta + C\,(m - X(t_{obs})\beta)} with \eqn{m} the posterior means at the
observed locations and \eqn{C} the kriging map of the fitted continuous prior (see
\code{\link[=krige_GP]{krige_GP()}}). For \code{method = "fem"} the coefficients of the finite representation
are recovered from \eqn{m}, which requires that they are determined by it (e.g. a
1-d Matern mesh whose nodes are the observed locations, or L-GP knots at a subset
of them).
}
