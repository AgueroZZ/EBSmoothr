% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_eb_smoother.R
\name{eb_smoother}
\alias{eb_smoother}
\title{Fit an empirical-Bayes smoother with known or learned noise}
\usage{
eb_smoother(
  x,
  s = NULL,
  family = c("matern", "constant", "point_exponential", "point_normal", "point_laplace",
    "lgp"),
  backend = c("auto", "exact", "laplace", "laplace_fisher", "fisher_pql", "inla",
    "inlabru"),
  locations = NULL,
  setup = NULL,
  g_init = NULL,
  fix_g = FALSE,
  beta_fixed = NULL,
  beta_prec = NULL,
  fix_params = character(),
  pc.penalty = NULL,
  penalty_range = NULL,
  alpha = 2,
  max.edge = NULL,
  matern_n_starts = 1L,
  pql_inner_iter = 3L,
  profile_s_lower = 1e-08,
  profile_s_upper = NULL,
  profile_s_tol = .Machine$double.eps^0.25,
  suppress_warnings = TRUE,
  compute_exact_diagnostic = FALSE,
  link = c("identity", "log", "softplus"),
  ...
)
}
\arguments{
\item{x}{Numeric vector of observations.}

\item{s}{Observation standard errors. Supply a scalar or length-\code{length(x)}
vector for known noise, or \code{NULL} to learn one common scalar SD.}

\item{family}{Smoother family. One of \code{"matern"}, \code{"constant"},
\code{"point_exponential"}, \code{"point_normal"}, \code{"point_laplace"}, or \code{"lgp"}.}

\item{backend}{Backend choice. For \code{family = "matern"}, \code{"auto"} uses the
exact Gaussian backend for \code{link = "identity"} and \code{"fisher_pql"} for
\code{link = "log"} and \code{link = "softplus"}. Matern also
supports explicit \code{"laplace"}, \code{"laplace_fisher"}, \code{"fisher_pql"},
\code{"inla"}, and \code{"inlabru"}. The \code{"fisher_pql"} backend is an approximate
Fisher/PQL backend for non-identity Matern fits. It uses
\code{pql_inner_iter} pseudo-Gaussian exact Matern Step A updates and reports a
Fisher/Laplace score evaluated at the final PQL mode, not a true
re-optimized original-model Laplace marginal likelihood. The \code{"inlabru"}
backend runs
the SPDE model through
\code{inlabru}'s iterative-linearised INLA method and is currently only
supported for \code{link = "softplus"}. It is an experimental validation
backend, not an expected acceleration path. For known-noise fits,
\code{pc.penalty = NULL} keeps the non-PC SPDE parameterisation; learned-noise
inlabru fits with \code{s = NULL} require an explicit \code{pc.penalty} list with
\code{range}, \code{sigma}, and \code{noise} entries.
Compatibility aliases \code{"laplace_tmb"} and \code{"inla_pc"} are still accepted.
Explicit \code{"laplace"}, \code{"laplace_tmb"}, and \code{"inla"} retain
observed-Hessian Laplace semantics for log-link and softplus-link fits
where supported. Public Matern Laplace backends use the package-owned TMB
implementation or error; the R reference backend \code{"laplace_r"} is retained
for internal validation only. For \code{family = "lgp"}, \code{"auto"} uses
\code{"tmb"} for the identity link, \code{"laplace_fisher"} for the log link, and
\code{"laplace"} for the softplus link. The \code{"constant"} and point-mass
reference families support only the exact backend.}

\item{locations}{Optional raw spatial locations for the Matern family.}

\item{setup}{Family-specific setup object:
\code{\link[=Matern_setup]{Matern_setup()}} for \code{family = "matern"} or \code{\link[=LGP_setup]{LGP_setup()}} for
\code{family = "lgp"}.}

\item{g_init}{Optional prior-family-specific initialization object.
When supported, \code{g_init$beta} supplies an initialization value for \code{beta}
and \code{g_init$beta_prec} can provide a fallback prior precision.}

\item{fix_g}{Logical. If \code{TRUE}, supported prior-family parameters in
\code{g_init} are held fixed while scoring or fitting. This is mainly useful
for permutation scoring with \code{family = "matern"} or a point-mass
reference family.}

\item{beta_fixed}{Optional fixed intercept. For \code{"matern"} and
\code{"constant"}, \code{NULL} means optimize the intercept by marginal
likelihood; a numeric scalar holds it fixed. For \code{"lgp"}, this should be
a numeric vector of length \code{ncol(setup$X)}.}

\item{beta_prec}{Optional non-negative prior precision on \code{beta}.
\code{NULL} means empirical-Bayes estimation, \code{0} requests a flat prior, and a
positive value requests a proper zero-mean Gaussian prior.}

\item{fix_params}{Character vector naming prior parameters to fix while
estimating the remaining parameters. For \code{"matern"}, allowed values are
\code{"range"}, \code{"sigma"}, and \code{"beta"}; \code{"range"} uses \code{g_init$theta},
\code{"sigma"} uses \code{g_init$sigma}, and \code{"beta"} uses \code{beta_fixed} when
supplied or otherwise \code{g_init$beta}. For \code{"lgp"}, allowed values are
\code{"scale"} and \code{"beta"}; \code{"scale"} uses \code{g_init$scale}. \code{fix_g = TRUE}
is retained as a shortcut for fixing Matern range/sigma or L-GP scale.}

\item{pc.penalty}{Optional Matern PC prior specification. Supported entries
are \code{range}, \code{sigma}, and, when \code{s = NULL}, \code{noise}.}

\item{penalty_range}{Initial range anchor for the Matern family when
\code{g_init$theta} is missing.}

\item{alpha}{Matern SPDE smoothness parameter.}

\item{max.edge}{Optional mesh size control for the Matern family. For
two-dimensional Matern fits with \code{max.edge = NULL}, the default keeps all
observed locations as mesh vertices and uses a coarser outer mesh for
screening-oriented fits.}

\item{matern_n_starts}{Number of Step A optimization starts for the
package-owned TMB Matern Laplace learned-noise path. The default is \code{1};
use \code{5} to reproduce the earlier multistart behavior.}

\item{pql_inner_iter}{Positive integer number of Fisher/PQL pseudo-Gaussian
Step A updates used by Matern \code{backend = "fisher_pql"} and by
\code{backend = "auto"} when it resolves to Fisher-PQL for log or softplus
Matern fits. The default is \code{3}.}

\item{profile_s_lower, profile_s_upper}{Lower and upper bounds for profiling
the scalar observation noise SD when \code{family} is one of the point-mass
reference families and \code{s = NULL}. If \code{profile_s_upper = NULL}, it is set
from the scale of \code{x}.}

\item{profile_s_tol}{Optimization tolerance for the profiled
point-mass-reference noise SD.}

\item{suppress_warnings}{If \code{TRUE}, suppress INLA mesh and SPDE warnings.}

\item{compute_exact_diagnostic}{If \code{TRUE}, store the exact Gaussian
log-likelihood evaluated at the Matern INLA mode.}

\item{link}{Link used by the selected family. The Matern family supports
\code{"identity"}, \code{"log"}, and \code{"softplus"} fits with known or learned noise.
The constant family supports \code{"identity"} and \code{"log"} constant-baseline
fits. The L-GP family supports \code{"identity"}, \code{"log"}, and \code{"softplus"}.
Softplus posterior moments are deterministic Gauss-Hermite transforms of
each marginal Gaussian Laplace posterior for the linear predictor.}

\item{...}{Additional arguments. \code{dll} is used for the learned-noise L-GP TMB
path. \code{mode}, \code{scale}, \code{output}, \code{optmethod}, and \code{control} are forwarded
to the selected \code{ebnm} point-mass family.}
}
\value{
An object of class \code{c("eb_smoother_fit", "list")}. Use \code{print(fit)}
for a compact overview and \code{summary(fit)} for posterior and diagnostic
summaries of the stored fit.
}
\description{
Provides a unified front-end for the package's smoothing families.
Use \code{eb_smoother()} as the high-level smoothing interface when you want to
fit one of the package's prior families directly.

The primary documented use case is learned-noise smoothing: when \code{s = NULL},
\code{eb_smoother()} estimates one common observation noise SD together with the
prior-family parameters. This is the main package entry point when standard
errors are not known in advance.

When \code{s} is supplied, \code{eb_smoother()} still supports the same smoothing
families with known observation standard errors. For known-\code{s} workflows
embedded in \code{ebnm}-compatible pipelines such as \code{flashier} and \code{ebmf}, the
lower-level \code{ebnm_LGP_generator()} and \code{ebnm_Matern_generator()} remain the
recommended interfaces.

For the \code{"matern"} and \code{"constant"} families, the intercept is optimized
by marginal likelihood by default. The exact Gaussian implementations may
profile that intercept analytically for efficiency, but this is
mathematically equivalent to joint optimization. Supplying \code{beta_fixed}
instead holds the intercept fixed, while \code{beta_prec} can be used to switch
to a flat (\code{0}) or proper (\verb{>0}) Gaussian prior on the intercept. These beta
semantics are on the linear-predictor scale for every link, including
\code{link = "log"} and \code{link = "softplus"}.
}
\examples{
loc <- seq(0, 1, length.out = 40)
x <- sin(2 * pi * loc) + rnorm(length(loc), sd = 0.1)
fit_learned <- eb_smoother(
  x,
  s = NULL,
  family = "matern",
  locations = loc,
  pc.penalty = list(range = 0.2, sigma = 0.3, noise = 0.1)
)
fit_learned
summary(fit_learned)
head(fit_learned$posterior)

fit_baseline <- eb_smoother(x, s = NULL, family = "constant", beta_prec = 0)
summary(fit_baseline)

x_pos <- exp(0.1 + 0.4 * sin(2 * pi * loc)) + rnorm(length(loc), sd = 0.1)
fit_positive <- eb_smoother(
  x_pos,
  s = NULL,
  family = "matern",
  locations = loc,
  link = "log"
)
fit_positive_baseline <- eb_smoother(x_pos, s = NULL, family = "constant", link = "log")

fit_sigma_fixed <- eb_smoother(
  x,
  s = 0.1,
  family = "matern",
  locations = loc,
  g_init = Matern(sigma = 1),
  fix_params = "sigma"
)

fit_sparse_baseline <- eb_smoother(x_pos, s = NULL, family = "point_exponential")
as.numeric(fit_positive$log_likelihood) - as.numeric(fit_sparse_baseline$log_likelihood)

}
